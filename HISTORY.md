# speakyspeak · HISTORY

Human-readable, append-only. Baseline generated from git log 2026-08-24; sessions append below, never rewrite old entries.

## 2026-06

The project started as SpeakySpeak, a spoken-reply queue panel for Claude Code, moved over from an earlier location at ~/.claude/tools/speakyspeak. Early work made the playback queue reliable: fixed sequencing races so a paused reply couldn't get overwritten by a new arrival, made session end stop deleting unheard replies, and persisted mute and playback rate across restarts so a relaunch would not blast a stale backlog. The power button became a real on/off control instead of quitting the app, queue rows became inert display-only items with icon-based status, and a footer was added with sweep and delete-all actions plus tooltips on every icon. Partway through the month the text-to-speech engine changed from macOS `say` to a local neural voice engine (Kokoro, via mlx-audio) with `say` kept as a fallback, and a volume slider with loudness normalization was added. Two hook bugs were fixed: the hook was speaking the mid-turn preamble instead of the final reply, and it could re-speak a stale reply when a transcript flushed late. Mid-month the floating panel moved into the macOS menu bar, and the app got its own brand mark ("Sy," a hand-made Optima-based icon) plus a settings window. The month closed with a mini HUD: quiet/skip controls, a global hotkey, and a dismiss button.

## 2026-07

The mini HUD grew into the app's main visible surface: full transport controls, drag-anywhere positioning, a "locate me" glow, a Liquid Glass visual treatment, a bright flash on track changes, a bottom progress bar, and a choice between "always visible" and "only while speaking." Emoji were stripped from spoken text so the voice engine would not try to read them aloud. A warm TTS daemon cut the time to render a new reply from about 3 seconds to about 0.5 seconds. The project got a public GitHub remote (radam5000/speakyspeak) and a SETUP.md written for a friends beta, meaning other people besides Adam could install it on a fresh machine. Settings gained a voice preview so a user could audition any voice before picking it, a launch crash was fixed, and the install script was made to reload the TTS daemon reliably.

Standout events:
- 2026-07-16: CLAUDE.md corrected to reflect that a public remote now exists.
- 2026-07-18: SETUP.md written specifically for the friends beta, the first sign the app was meant for people other than Adam.
- 2026-07-20: Voice preview added to Settings, plus a fix for a launch crash caused by loading the voice list on the main thread.

## 2026-08

This was the release month. The app shipped its first public version, built a full site, stood up a real bug-feedback loop, and moved through more than twenty numbered releases by month end.

Early in the month the app was made to speak a whole reply turn rather than just the text after the last tool call, a minimum-macOS-version mismatch was fixed (the binary required 26.0 while the plist advertised 14.0), and the README was rewritten for a public MIT-licensed release. AirPods got direct support: stem clicks control playback via Now Playing, and a "peer gate" was added so two Macs sharing one pair of AirPods take turns instead of fighting over them.

The bulk of the month went into the public website and a long string of design iterations: a rainbow "aurora" ripple effect on the HUD went through several redesign passes, a public mirror repo and installer/updater plumbing shipped as v1.0.1, and the site itself went through many rounds (hero copy, sticker galleries, mobile layout, an accessibility pass, and an em-dash purge of the copy). Adam set the rule that site copy must be written in his own voice with no em dashes, and that any subagent writing copy must be told this explicitly. A real bug-feedback loop went live on 2026-08-18: a support email, a verify.sh script with fixture tests, and a daily triage job under launchd. That day's polish releases were rolled into 1.1.0 with a user-facing CHANGELOG. In the last few days of the month the queue gained session-run grouping and a user-settable sort order (1.2.0), a public GUIDE.md was published (1.2.1), the menu-bar icon was fixed to hold still while its badge updates (1.2.2), an AirPods audio lead-in was added and confirmed on a real two-Mac setup (1.2.3), and a legibility and Settings-layout pass shipped as 1.2.4, followed by a deploy script added after a short self-inflicted site outage.

Standout events:
- 2026-08-16: v1.0.1 released along with a public site, an updater, and Lily as the shipped default voice.
- 2026-08-17: A single very long day of site design iteration, ending in an accessibility audit, an em-dash purge, and versions 1.0.2 through 1.0.6.
- 2026-08-18: The feedback loop (hi@speakyspeak.com, verify.sh, daily triage) went live, and the day's fixes rolled up into 1.1.0.
- 2026-08-23: v1.2.0 shipped (session-run ordering, sort control, a fix for lost replies), followed by 1.2.1 through 1.2.3 the same day.
- 2026-08-24: v1.2.4 shipped (panel legibility, one menu-bar mark, three-section Settings), and a deploy script was added after a brief self-inflicted outage.

## 2026-09

The 2026-08 section above was written on 8/24 and stops at 8/25, so this section picks up from 8/26.

Late August was small and mostly bookkeeping. On 8/26 speakyspeak.com was wired into the shared PostHog project so the site finally reports its own visitors. On 8/29 and 8/30 a transcript-mining pass read back old sessions and filed five things nobody had written down anywhere, including a claim on the site that Claude Code on the web can work with the app (it cannot, because the Stop hook fires in Anthropic's cloud sandbox and not on the user's Mac) and Adam's one stated design criterion.

Then two releases on 9/03. 1.2.8 speaks permission prompts: the hook now runs on Claude Code's Notification event, plays a distinct chime, and reads "Waiting on you in <session>" through the deck. Adam tested it the same day and said it runs great. 1.2.9 made install.sh register all four hook events in `~/.claude/settings.json` itself, so an in-app update carries 1.2.8's new hook to existing installs with nothing to paste by hand, and added a "What's new" row in Settings. The same day Adam decided the feedback loop stays in triage mode rather than being promoted to drafting fixes, his reason being that both loops have been working fine as they are.

Standout events:
- **2026-08-28, a correction that is worth keeping.** A morning entry that day claimed ss-hud-display-change (the mini player not coming back to the built-in screen after an external display is unplugged) had been an untested hypothesis until a lost 8/19 voice memo was recovered. That was wrong on every count. Commit `aceeccb` of 8/21 already cites the memo by date, already diagnoses the cause exactly (the panel's positioning is only run when it is shown, so in always-visible mode nothing re-evaluates the frame after a display disconnect, and the Mission Control flash is the panel rendering off-layout), and already ships the fix (a screen-parameters observer that re-runs positioning, two passes 0.4s apart). That commit even notes the memo had sat unrouted in transcription staging for two days. The only thing genuinely missing was the vault journal note. The item's real state never changed: the fix is written and shipped, and it needs two minutes of Adam's hardware to confirm. Moved here from the TODO line 2026-09-05.
- **2026-09-03: 1.2.8 and 1.2.9 both shipped**, and the feedback loop's trust gate was decided (stays at triage).
- **2026-09-05, a repo hygiene pass.** GOAL.md got a Roadmap (now / next / ideas shelf), which is what the project dashboard reads for its ladder, and Adam's one stated design line from 8/18 is now written down there as a dated quote instead of living only in a transcript. TODO.md's two overlong items were cut back to current state, the 8/28 correction moved into this file, and every open line now carries a mark saying whose move it is. SETUP.md was retired into a stub: it was the 7/18 friends-beta walkthrough, INSTALL.md had superseded it, and it had drifted to describing two hook events when the app has needed four since 1.2.8, which mattered because the public mirror ships it. Its one unique section, uninstall, moved into INSTALL.md. README's install paragraph no longer tells people to hand-add hook entries, because install.sh has registered them itself since 1.2.9.
- **2026-09-05, second pass, from a doc census.** README's `## Status (2026-06-22)` section was a three-month-dead changelog-by-hand that CHANGELOG.md and this file had long since taken over; it is now a two-line pointer that keeps the one thing only it knew, which is where the pre-repo dev history lives. PLAN-LAUNCH.md's "ground truth as of writing" block still claimed VERSION 1.0.5, no feedback channel, no tests, and no VoiceOver work. That block stays exactly as written, because the plan was built on it, but a dated correction now sits under it. This matters more than it looks: the daily feedback loop's prompt points agents at PLAN-LAUNCH.md as the design of the thing they are running, so a stale ground-truth header there is read by a machine every morning. The census also found real duplication between README's Settings and Ordering sections and GUIDE's, left alone deliberately: they serve two different readers and verify.sh gates GUIDE on specific strings.
- **2026-09-05, 1.2.10, the first two-Mac bug.** Adam noticed the Pro deck was not moving to the next reply, and restarting it did not help. The stuck state was on the other machine: the Air's deck had started a reply at 14:59, the lid closed 28 seconds in, and the audio player froze without ever reporting a finish, so the Air kept answering "playing" on the peer port and the Pro, which asks its peer before every automatic start, held for over an hour. Two changes: the peer answer now means sound is actually moving rather than a flag being set, and the deck watches its own progress and files a reply frozen for about ten seconds as played. Deliberately no auto-advance from a stall, so a Mac in dark wake cannot chew through its queue in silence. Diagnosed from deck.log on both machines plus the Air's pmset log; the fix was pushed to the mirror; the Air was asleep, so a retry script installed it over SSH the moment it woke (16:25, third attempt); its peer port answered "idle" right after.
- **2026-09-11, 1.2.11, the other-setups pass.** Started from Adam's question after a morning of iTerm2 tab cosmetics: does his setup-specific tinkering make a mess for users with other terminals? Answer, from the code: no, the hook never mentions iTerm2 and the site already frames the colored-windows setup as optional. The real cross-setup gaps were four and are now fixed or stated: the build floor (docs said macOS 14, the glass code needs the macOS 26 SDK, so 15.6 is the truth and Adam chose to raise the stated minimum rather than guard the code), the hook's bare-PATH jq failure under Dock-launched Claude Code, the English-only neural voice, and the site's "wherever Claude Code runs" overclaim (web and SSH named as out). Decision recorded: Adam declined friend installs on other setups for now ("figure it out on your own"), so the fixture test under a bare PATH is the stand-in for a stranger's Mac.

## 2026-09-12 · sweep: the Air's 1.2.11 update fails under Command Line Tools 27.0

Adam reported from the Air at 08:57 ("The in-app update won't complete… rc=1") to hi@speakyspeak.com. The sweep read the Air's /tmp/claude-speech/update.log: the pull to 1.2.11 succeeded, then swiftc failed with 96 errors, the first being `external macro implementation type 'SwiftUIMacros.StateMacro' could not be found … plugin for module 'SwiftUIMacros' not found`; every later "'self' is immutable" error is that one cascading. Software Update had installed Command Line Tools for Xcode 27.0 on the Air at 08:53 that morning (pkgutil install-time; the Air was on CLT 26.6 since 7/13 and built 1.2.10 fine on 9/05). The Pro got CLT 27.0 on 9/11 11:16 but is unaffected because xcode-select points at Xcode 26.6, whose Platforms tree ships libSwiftUIMacros.dylib; the CLT has no such plugin. Upstream match: drumih/turbo-fieldfare#121 (Swift 6.4, same error, resolved there only by selecting full Xcode). Verified on the Air in a scratch copy: `swiftc -typecheck -sdk /Library/Developer/CommandLineTools/SDKs/MacOSX26.5.sdk -target arm64-apple-macosx14.0 main.swift` exits 0 with zero errors; the default SDK 27 typecheck gives 96. CLT 27.0 still ships MacOSX26.5.sdk, so the fix is in build.sh (choose an SDK whose SwiftUI still typechecks), plus INSTALL step 0 (its `>= 26` check passes 27) and a verify.sh fixture. Not done in the sweep: the release itself (Adam's go). The Air is left as found: clone at 1.2.11, app 1.2.10, deck running, updater will re-offer and re-fail until 1.2.12.

Also found the same morning: the three headless launchd Claude runs (05:00 sweep-all, 07:30 markemark loop, 07:45 feedback loop) are read aloud through the deck in "every line" mode, 9 spoken lines today; filed as ss-headless-runs-spoken for Adam's call, no decision existed either way.

## 2026-09-12 · 1.2.12 shipped: the build survives Command Line Tools 27.0

Adam said "go" on the sweep's finding above. Fix: `scripts/pick-sdk.sh` compiles a one-line @State probe against the default SDK and, when that fails, walks the MacOSX*.sdk entries beside it newest first (aliases deduped by real path, anything below SDK 26 skipped) and hands build.sh the first that works; a healthy toolchain gets an empty answer and builds exactly as before. Thirteen contract tests with a fake swiftc and a CLT-shaped fixture directory run in verify.sh step 3. INSTALL step 0 lists the SDK directory and requires a MacOSX26 entry; README and CHANGELOG say why. Proven on the Air's real 27.0 tools in a scratch dir (picker chose MacOSX26.5.sdk, full -O compile exit 0), then verify.sh ALL GREEN on the Pro, mirror snapshot published (VERSION 1.2.12), and the Air updated over SSH the way its updater would (git pull, install.sh): build fell back as designed, deck relaunched on 1.2.12 at 10:00 EDT. The Pro's running deck was left alone: build.sh put 1.2.12 in ~/Applications during verify.sh, and main.swift did not change, so relaunching would change nothing. Left open: a Mac whose tools carry no macOS 26 SDK at all cannot build; the picker prints the two ways out instead of failing mid-compile.

## 2026-09-12 · 1.2.13: the menu-bar mark could launch invisible

Right after the 1.2.12 relaunch Adam sent three Air screenshots: the Sy missing from the dark menu bar, visible only on the highlighted pill when clicked, Settings otherwise fine on 1.2.12. The mark is not a template (the orange queue count needs a second colour since 1.2.5), so it is tinted by hand with the label colour resolved inside the status button's effective appearance. That appearance is not settled when the first refreshStatus() runs at launch, and afterwards only queue, mute, current-reply, update and Light/Dark-toggle events redraw it. The SSH relaunch landed in an empty queue, so nothing redrew for twenty minutes; every earlier launch had had a reply within seconds, which is why it was never seen before. Evidence before the fix: posting AppleInterfaceThemeChangedNotification from the Air's shell redrew the icon once and Adam confirmed the Sy reappeared. Fix: KVO on the button's effectiveAppearance calling refreshStatus(), which also covers wallpaper-driven menu-bar tone changes that never post the theme notification. Proven by repeating the exact scenario (SSH pull, install, `open`, empty queue) on 1.2.13; Adam: visible. The Pro's deck was left on 1.2.11 for Adam to update (it is paused mid-reply with a queue). A side finding, filed nowhere yet because it is harmless: an app opened with `open` from an SSH shell inherits SSH_CLIENT and SSH_CONNECTION in its environment.

